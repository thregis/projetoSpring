import DeleteIcon from '@material-ui/icons/Delete';
import {Button} from '@material-ui/core'

const ButtonRemove = () => {
    return(
<Button variant="contained" color="secondary"><DeleteIcon/>Remover</Button>
    )
}

export default ButtonRemove